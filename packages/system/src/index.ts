import {
  BorderProps,
  ColorProps,
  FontSizeProps,
  LayoutProps,
  ResponsiveValue,
  SpaceProps,
  variant,
  compose,
  border,
  color,
  fontSize,
  layout,
  space,
} from 'styled-system'

export type {
  BorderProps,
  ColorProps,
  FontSizeProps,
  LayoutProps,
  ResponsiveValue,
  SpaceProps,
}

export { variant, compose, border, color, fontSize, layout, space }
